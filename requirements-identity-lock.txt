# Reproducibility lock for the identity-audit layer on the current Windows lane.
# The audit and tests themselves use the Python standard library; model lanes
# must install the project-specific GPU stack separately without committing
# model weights or caches.
python==3.11.9
numpy==2.4.6
torch==2.14.0+cu126
torchaudio==2.11.0+cu126
speechbrain==1.1.1
huggingface-hub==1.30.0
